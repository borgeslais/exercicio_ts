// Função de multiplicação
function multiplica(a: number, b: number): number {
    return a * b;
}

// Função de saudação
function ola(nome: string): string {
    return "Olá " + nome;
}

console.log(multiplica(6, 8));
console.log(ola("Ana"));
